const tasks=[];
tasks.push('Play Piano');
tasks.push('Play GTA5');
tasks.push('Record Videos');
tasks.push('Awesome Person');
tasks.forEach
(
	function(task,i)
	{
		console.log(`Your task number ${i+1} is ${task}`);
	}
)	